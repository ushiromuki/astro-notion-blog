---
import * as interfaces from '../../lib/interfaces.ts'
import Mention from './Mention'

export interface Props {
  block: interfaces.Block
}

const { block } = Astro.props
---

<p>
  <Mention pageId={block.LinkToPage?.PageId} client:only="react"/>
</p>
